{
   "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" }
  ]
  "routes": [
    {
      "src": "/db.json",
      "dest": "/db.json"
    }
  ]
  "build": {
    "env": {
      "NODE_OPTIONS": "--openssl-legacy-provider"

    }
}

  }
}
